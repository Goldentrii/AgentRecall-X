node_modules/
dist/
*.tsbuildinfo
.DS_Store
.superpowers/

# Claude worktrees
.claude/

# Stray src copies
/src/
supabase/.temp/
.superpowers/

# MCP registry publishing — secrets + downloaded binary (never commit)
.mcpregistry_*token
mcp-publisher

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Real-corpus eval data — contains real user paths/names/project slugs; never commit
scripts/eval/fixtures/loop8-labeled-rejects.json

# agent-loop runner artifacts (repo-root runs): transcript/scratch state + kill switch
/state/
/STOP

# Golden-query retrieval eval — fixture + lock + baseline runs embed the
# owner's private memory content near-verbatim (rules, live-store excerpts,
# client project slugs) and are, BY DESIGN, coupled to the private store
# (source_check_regex / golden_source_path), so the baselines/local/
# anonymization convention cannot apply. Never commit any of it (fix4
# independent review H1, 2026-09-11); the runner + README ride along until
# an anonymized public variant is deliberately prepared.
scripts/eval/golden-queries/
